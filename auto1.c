#include<stdio.h>

//global variable by default is intialize to 0
int var;

int main(){
    printf("%d\n",var);
//local variables is intialized to garbage value by default 

int a;
printf(a);

//extern modifier

}